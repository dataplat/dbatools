# The single VMSS fleet controller. CI requests, per-job completion nudges, maintainer
# pushes, and the five-minute safety tick all reconcile the same desired state.
#
# AppVeyor-style pool policy:
#   - potatoqualitee: 10 dedicated workers for one hour after a PR or [do ci]
#   - andreasjordan:  10 dedicated workers for one hour after activity
#   - niphlod:         10 dedicated workers for one hour after activity
#   - community:       5 shared workers while CI is live and for 20 minutes after
#   - no activity or queued work: zero workers
#
# Each Flexible VMSS instance stores its pool in the runnerPool Azure tag and the
# matching GitHub runner label. Ephemeral agents take one job, unregister, and the
# controller replaces their VM without letting another pool steal the slot.
name: runner-reconcile

on:
  schedule:
    - cron: "*/5 * * * *"
    - cron: "0 7 * * 1"
  workflow_run:
    workflows: ["ci-azure"]
    types: [requested, completed]
  workflow_dispatch:
    inputs:
      boost_user:
        description: "Actor whose activity should heat their pool immediately"
        required: false
        default: ""
      boost_message:
        description: "Head commit message used to evaluate the runner activation marker"
        required: false
        default: ""
      boost_sha:
        description: "Head commit SHA used to deduplicate marked CI dispatches"
        required: false
        default: ""
      boost_ref:
        description: "Branch ref used when dispatching marked CI"
        required: false
        default: ""

permissions:
  contents: read
  actions: write
  issues: write
  id-token: write

# Reconcile is idempotent. Coalescing redundant pending nudges is intentional; the
# running pass reads the complete GitHub queue rather than relying on one event body.
concurrency:
  group: runner-fleet
  cancel-in-progress: false

jobs:
  reconcile:
    if: github.ref == 'refs/heads/development' && github.event.schedule != '0 7 * * 1'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      GH_TOKEN: ${{ secrets.CI_RUNNER_PAT }}
      REPO: ${{ github.repository }}
      RG: ${{ vars.CI_AZURE_RG }}
      VMSS: ${{ vars.CI_VMSS_NAME }}
      MAX_RUNNERS: 35
      COMMUNITY_COUNT: 5
      COMMUNITY_GRACE_MINUTES: 20
      BOOST_USERS: potatoqualitee andreasjordan niphlod
      BOOST_COUNT: 10
      BOOST_HOURS: 1
      OPT_IN_PUSH_USERS: potatoqualitee
      CI_MARKER: "[do ci]"
      BOOST_TRIGGER: ${{ inputs.boost_user }}
      BOOST_MESSAGE: ${{ inputs.boost_message }}
      BOOST_SHA: ${{ inputs.boost_sha }}
      BOOST_REF: ${{ inputs.boost_ref }}
      WORKFLOW_TOKEN: ${{ github.token }}
      RUNNER_LABEL: dbatools-modern
      RAW_BASE: https://raw.githubusercontent.com/${{ github.repository }}/${{ github.ref_name }}
      POLICY_PATH: /tmp/runner-policy.ps1
      BOOTSTRAP_PATH: /tmp/bootstrap-runner.ps1
    steps:
      - name: Azure login (OIDC)
        uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Download fleet controller
        id: controller
        run: |
          set -euo pipefail
          echo "ready=false" >> "$GITHUB_OUTPUT"
          if ! curl --retry 3 --retry-all-errors --retry-delay 3 -fsSL \
              "$RAW_BASE/.github/runners/reconcile-runner-fleet.ps1" \
              -o /tmp/reconcile-runner-fleet.ps1; then
            echo "::warning::Fleet controller download exhausted retries; the next nudge or scheduled tick will retry."
            exit 0
          fi
          if ! curl --retry 3 --retry-all-errors --retry-delay 3 -fsSL \
              "$RAW_BASE/.github/runners/runner-policy.ps1" \
              -o "$POLICY_PATH"; then
            echo "::warning::Runner policy download exhausted retries; the next nudge or scheduled tick will retry."
            exit 0
          fi
          if ! curl --retry 3 --retry-all-errors --retry-delay 3 -fsSL \
              "$RAW_BASE/.github/runners/bootstrap-runner.ps1" \
              -o "$BOOTSTRAP_PATH"; then
            echo "::warning::Runner bootstrap download exhausted retries; the next nudge or scheduled tick will retry."
            exit 0
          fi
          echo "ready=true" >> "$GITHUB_OUTPUT"

      - name: Reconcile AppVeyor-style pools
        if: steps.controller.outputs.ready == 'true'
        shell: pwsh
        run: /tmp/reconcile-runner-fleet.ps1

  spend-report:
    if: github.event.schedule == '0 7 * * 1'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      GH_TOKEN: ${{ secrets.CI_RUNNER_PAT }}
      REPO: ${{ github.repository }}
      RG: ${{ vars.CI_AZURE_RG }}
    permissions:
      issues: write
      id-token: write
      contents: read
    steps:
      - name: Azure login (OIDC)
        uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Post month-to-date spend
        run: |
          set -euo pipefail
          SUB=$(az account show --query id -o tsv)
          FROM=$(date -u +%Y-%m-01)
          TO=$(date -u +%Y-%m-%d)
          BODY=$(az rest --method post \
            --uri "https://management.azure.com/subscriptions/$SUB/resourceGroups/$RG/providers/Microsoft.CostManagement/query?api-version=2023-11-01" \
            --body "{\"type\": \"ActualCost\", \"timeframe\": \"Custom\", \"timePeriod\": {\"from\": \"$FROM\", \"to\": \"$TO\"}, \"dataset\": {\"granularity\": \"None\", \"aggregation\": {\"totalCost\": {\"name\": \"Cost\", \"function\": \"Sum\"}}}}" \
            --query "properties.rows[0][0]" -o tsv 2>/dev/null || echo "unavailable")
          COST=$(printf "%.2f" "$BODY" 2>/dev/null || echo "$BODY")
          ISSUE=$(gh issue list -R "$REPO" --search "CI cost tracker in:title" --state open --json number --jq '.[0].number')
          if [ -z "$ISSUE" ]; then
            ISSUE=$(gh issue create -R "$REPO" --title "CI cost tracker" \
              --body "Automated month-to-date Azure spend for the self-hosted runner fleet." \
              | grep -o '[0-9]*$')
          fi
          gh issue comment "$ISSUE" -R "$REPO" --body "Month-to-date spend for \`$RG\` as of $TO: **\$$COST**"
